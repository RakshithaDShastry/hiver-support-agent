# Only needed for the OPTIONAL full pipeline regeneration path.
# Not needed for the fast metrics reproduction path (scripts/compute_metrics.py).
# Copy this file to .env and fill in your real key. NEVER commit the real .env file.
GROQ_API_KEY=your-groq-key-here
