# Core (needed for the fast metrics reproduction path — scripts/compute_metrics.py)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Only needed for the OPTIONAL full pipeline regeneration path
# (re-calling the LLM APIs from scratch — see README)
groq>=0.9
kagglehub>=0.3
tqdm>=4.66
python-dotenv>=1.0
